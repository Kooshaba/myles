.registration__container
  .registration__header.column-12
    .registration__header-logo.inline-block.mr10
      = render "svg_html/logo", icon_color: "#fff"
    You're all set!

  .p25.text-center
    %h1
      We're going to set up
      %span.main-color #{@recipient.first_name.capitalize}'s
      custom tablet and send it once it's ready.

    %p.mtb50 We'll let you know once they start using it.

    = link_to "View Settings", settings_path, class: "btn"
